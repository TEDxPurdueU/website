node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.DS_Store

# Raw camera files (300MB+), including the team headshots under team/.
# Optimised output in static/img and static/headshots is committed; regenerate
# with `npm run images` and `npm run headshots`. Keep these backed up
# elsewhere.
_originals/

# Local Netlify folder
.netlify
.vercel

# Machine-local Conductor settings (lives in the repo root .conductor/)
.conductor/settings.local.toml

# Prod builds from package-lock.json (Vercel runs npm). A local `pnpm install`
# regenerates this one, and Vercel would prefer it over the npm lockfile if it
# ever landed — so it stays out of the repo.
pnpm-lock.yaml
