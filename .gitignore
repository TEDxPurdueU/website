node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.DS_Store

# Raw camera files (300MB+). Optimised output in static/img is committed;
# regenerate it with `npm run images`. Keep these backed up elsewhere.
_originals/

# Local Netlify folder
.netlify
